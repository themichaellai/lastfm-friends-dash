last.fm friends dashboard
=========================
A dashboard to show what friends are listening to.

To build this, run `npm install` and `npm run deploy` or `npm run start`. The
built app is in the `dist` folder.
